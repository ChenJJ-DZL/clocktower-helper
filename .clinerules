# Cline 项目治理与成本优化协议 (Board Game Engine Refactor)

## 1. 自动化授权 (Autonomous Authority)
- **免点 SAVE**: 你已获得完全授权，可以直接写入文件、创建新文件及执行终端命令。无需等待我的手动确认或点击 "SAVE"。
- **风险自负**: 我已开启 Git 协作与 VS Code 自动保存，你可以自由迭代。如遇严重错误，我会通过 Git 回滚。
- **静默执行**: 优先完成任务，仅在遇到无法通过本地工具解决的逻辑冲突时才中断提问。

## 2. Token 成本控制策略 (Token-Saving Tactics)
- **本地验证优先**: 禁止通过“猜测”或“反复对话”来排查语法错误。
  - 修改代码后，必须**自动执行** `npm run check:all`。
  - 只有当本地检查工具（TSC, Biome, Vitest）报错且你尝试修复 2 次失败后，才允许向我请示。
- **精简上下文**: 
  - 在读取文件前，先使用 `ls` 或 `grep` 定位关键逻辑，避免一次性读取超大文件。
  - 对话回复保持极致简洁，禁止输出解释性的废话，仅说明“做了什么”和“检查结果”。
- **利用自动压缩**: 既然已开启 Auto-compact，请尽量在单次 Turn 中完成“修改-检查-修复”的闭环，减少历史记录堆积。

## 3. 架构约束 (Architectural Constraints)
- **原子快照模式**: 所有的 State 修改必须遵循 Immutability（不可变性）。如果你发现代码中有直接修改属性的行为（Mutation），必须立即指出并重构。
- **OO 与解耦**: 严格监控循环引用。每当引入新的类引用，必须运行 `npm run circular`。
- **测试驱动**: 任何新功能的实现必须同步配套 `vitest` 测试用例。没有通过测试的代码视为无效交付。

## 4. 运行环境优化
- **环境意识**: 当前运行在 [M4 Mac mini / i5 PC] 环境下。
- **性能平衡**: 优先使用 Biome 进行格式化（比 ESLint 快且省资源）。
- **缓存利用**: 充分利用本地 `node_modules` 中的缓存，避免重复安装依赖。

## 5. 自动化测试与修复循环准则 (Auto-Fix Loop Rules)
1. **测试驱动**：当被要求进行自动化测试和修复时，优先运行测试命令（如 `npx playwright test` 或 `npm run test`）。
2. **分析与闭环**：如果测试失败，仔细阅读终端报错信息，定位具体的代码文件和行数，进行修复。
3. **自我验证**：修复后，**必须自动再次运行测试命令**验证修复结果。
4. **禁止等待**：不要询问用户“这样修改可以吗？”或“接下来要继续吗？”，请直接使用工具链执行修改并再次测试。
5. **循环终止条件**：直到所有测试用例 100% 通过，或者你认为遇到了系统级无法解决的死锁环境问题时，才能停止并向用户汇报。