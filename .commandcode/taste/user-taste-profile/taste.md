- Prefers plan/spec/implementation documents to live inside the project directory (e.g., `tests/plans/`, or other project-internal locations) rather than in global user-level directories like `~/.commandcode/plans/`. Rationale: keeps the plan colocated with the code/test files it describes, so it can be archived as a change record or deleted together with the project files after execution. Confidence: 0.9
- When a multi-step plan's remaining steps exceed reasonable scope for one session, accepts explicit scope reduction: bundle 2-3 steps into a simplified pass, defer UI/cosmetic work to future PRs, and document deferred items in a "范围外（后续 PR）" section in CHANGELOG rather than silently skipping. User said "continue继续" without pushback when agent did this — implicit approval pattern. Confidence: 0.85
- Triggers full execution with terse "开始执行" (start executing) after a detailed plan is approved — expects the agent to immediately move through the plan's TODO list step-by-step, with per-step verification (test/build) before moving on. Does not want a re-summary of the plan; wants action. Confidence: 0.9
- Strict project-protection rule: the `json/` directory is protected by `.clinerules` — must NOT modify any file in `json/` without explicit per-task user authorization. When the plan would touch `json/`, the agent must proactively request authorization via `ask_user_question` with concrete options, not silently bypass. Confidence: 0.95
- When `json/` is protected, prefers creating parallel data files in `src/data/` (e.g., `poppyganda_official_extras.json`) to supplement missing data, rather than skipping the feature entirely. The fallback file is loaded by extending the existing lookup utility's index maps (additive merge), not by rewriting the loader. Confidence: 0.9
- Avoids web/network fetches in the agent loop (web_fetch is treated as expensive/unreliable) — prefers copying data from already-checked-in spec docs (e.g., `docs/poppyganda_official_spec.md`) into local data files when external data is missing. Will explicitly note when content is missing from the local spec ("spec 文档未收录") rather than fabricating. Confidence: 0.9
- Records a baseline at execution start: explicit snapshot of `lint` / `type` / `test` / `build` exit states with known pre-existing errors tagged as "history" (not blockers). New changes must not regress these baselines. The baseline is used as a "before/after" comparison point for each step. Confidence: 0.9
- Treats pre-existing lint errors as scope-creep risk — explicitly decides NOT to clean them up ("避免范围蔓延") but commits to ensuring new changes do not introduce NEW lint errors. Maintains a clear "in-scope vs out-of-scope" boundary. Confidence: 0.9
- When the project's npm scripts have shell-injection issues (e.g., `"type": "tsc --noEmit ; echo EXIT=$?"`), the agent must work around them by invoking the underlying tool directly (`npx tsc --noEmit` via PowerShell) rather than trying to fix the script mid-execution. Logs the workaround so the user knows. Confidence: 0.85
- PowerShell is the only reliable shell for command execution in this Windows environment — bash-style `&&` chains and `npx` arguments get mangled by cmd. The agent must wrap any multi-step shell command in `powershell -NoProfile -Command "..."` for stability. Confidence: 0.9
- When a vitest test reveals that the SPEC assumption itself was wrong (e.g., "酒鬼在涡流局得正确信息" vs. "酒鬼 charadeRole=townsfolk → should be reversed"), stops to re-analyze the rule rather than force-fitting the test. Corrects both the spec interpretation and the test together. Confidence: 0.9
- After each step completes, the agent gives a single-line progress marker like "步骤 N 完成 ✅" with the test count (e.g., "89/89 套件 / 772 用例仍全绿") — terse, quantitative, no verbose summary. The next-step preamble lists the next 1-2 sub-tasks concretely. Confidence: 0.9
- During execution, skips `todo_write` when the tool misbehaves (schema errors / wrapper-tag confusion) rather than fighting the tool for 10+ rounds. Falls back to inline progress narration ("直接开始执行 + 每步完成时给一行总结"). Pragmatic tool usage over rigid process compliance. Confidence: 0.9
- The `tests/plans/` directory is a working scratchpad for plans — the user explicitly says "后期执行完成后作为测试文件一并归档或者删除" (archive or delete after execution). Plans in this directory are NOT permanent artifacts; they are lifecycle-managed. Confidence: 0.9# User Taste Profile
- Communicates exclusively in Simplified Chinese (简体中文). All responses, commit messages, and UI text should be in Chinese. Confidence: 0.95
- Uses terse, direct bug reports with numbered lists and "立即修复/立即解决" for urgency. Does not soften requests. Confidence: 0.9
- Uses "继续" to mean "keep going with the current task without re-summarizing." Confidence: 0.9
- Says "上传GitHub" as a shorthand to trigger the full commit + push workflow for both main and test repos. Confidence: 0.9
- Strongly values real-world usage testing over unit-test-only validation ("测试通过但用户实际使用时有BUG"). Prefers randomized automated UI testing via Playwright-style scripts against all built-in scripts. Confidence: 0.95
- Wants full utilization of machine resources (CPU/RAM) when running tests — does not want conservative resource usage. Confidence: 0.9
- Expects a standard verification pipeline after every fix: `npx tsc --noEmit` → `npm test` → `npm run build` → FAST_TEST run. Confidence: 0.9
- Prefers iterative rapid bug-fix cycles: find bug → fix immediately → verify → next bug. Does not batch fixes. Confidence: 0.9
- E2E "Loop Until Green" methodology: expects a fix-retest loop where any E2E failure triggers immediate frontend fix, then full E2E re-run until 100% pass. "修复后重新运行 E2E 测试，直至完整游戏流程 100% 跑通并全部通过." Confidence: 0.9
- After all fixes are done, expects the agent to handle git commit + push for both main repo and test_automation submodule without being asked. Confidence: 0.85
- Expects git tags to match commit message version numbers exactly — a W8.19.1 commit tagged as W8.19.0 is unacceptable. Agent must verify version consistency between tags and commit messages before pushing. Confidence: 0.9
- Expects local and remote (GitHub) to be perfectly synchronized after each upload — "确保云端与本地当前的内容相同". Any drift is treated as a bug. Confidence: 0.9
- Uses version scheme "W{major}.{minor}.{patch}" (e.g., "W8.14.15") in commit messages. Format: "W{ver}: {中文描述}" Confidence: 0.9
- Keeps test automation as a separate GitHub repo (`clocktower-helper-test-automation`) referenced as a git submodule. Confidence: 0.95
- Prefers committing both repos (main + test_automation) together when uploading to GitHub. Confidence: 0.85
- Strongly dislikes native browser dialogs (window.alert / window.confirm / prompt) — all must be replaced with styled, consistent React modals. Confidence: 0.95
- UI consistency is critical: no mixing of native OS/browser UI with custom-styled components. Confidence: 0.95
- Modal content formatting should use multi-line layout with labels on one line and values on a new centered line for readability (e.g., role name centered below the description). Confidence: 0.85
- Code comments and user-facing strings must be in Chinese. Confidence: 0.9
- Prefers structured logging (addLog/gameLogs) over console.log for debugging game state. Confidence: 0.8
- Windows environment (C:\Users\chenj\...), uses PowerShell for shell commands. Confidence: 0.95
- Project uses Next.js + React 19 + TypeScript + Vitest + Biome. Confidence: 0.95
- Delivers task specifications as detailed, structured docs with 【】section headers, numbered principles, explicit anti-patterns ("严禁..."), and reference URLs. Expects the agent to follow these specs precisely. Confidence: 0.9
- Strict architectural rule: no game-logic settlement / special-case code in UI/View components. All role abilities, status effects (poison/drunk/death), and night action ordering must live in a pure logic/engine layer. Confidence: 0.95
- TDD anti-pattern: "严禁通过删改现有测试断言来'假装通过'" — never delete or weaken existing test assertions to make tests pass. Tests are the source of truth; fix the code, not the tests. Confidence: 0.95
- Expects official game data (roles, teams, night order, jinxes) to be validated against authoritative external sources (e.g., wiki, JSON data packs) rather than assumed correct. Confidence: 0.9
- Delivers multi-phase task specs with "第一部分/第二部分" structure and explicitly requests "Loop Until Green" for each phase independently. Confidence: 0.9
- When a bug pattern is found in one file (e.g., `!r?.targetId` falsy check), expects a global scan and batch fix of all files with the same pattern — not a single-file patch. Confidence: 0.9
- Expects game state persistence across page refresh (LocalStorage snapshot save + auto-restore on mount). A "refresh at night mid-phase" must recover without white-screen or lost game. However, auto-restore must NOT skip the script selection page — the app should always open to script selection, with an explicit "继续上局" prompt card letting the user choose to resume or dismiss. User consent is required before restoring a previous session. Confidence: 0.95
- Production readiness checklist: zero console.log noise, `npm run build` with 0 errors/0 warnings, standalone Dockerfile, and TypeScript strict check all must pass before commit. Confidence: 0.9
- Stress/integration tests (20+ game simulations) must be vitest-ified (not just CLI scripts) and committed alongside the test runner so CI can reproduce. Confidence: 0.85
- Uses Playwright E2E scripts in `test_automation/` directory for real-browser validation (not just headless unit tests). Expects screenshot capture at key checkpoints. Confidence: 0.9
- Prefers pragmatic test adaptations (e.g., relaxing invariant checks that catch known framework bugs) over blocking on false positives, then fixing the framework separately. Confidence: 0.85
- Expects agent to proactively clean up debug/temp files (debug_*.ts, *_debug.txt) and unused imports before committing. No leftover scratch files in the repo. Confidence: 0.85
- When a bug affects multiple ability files, expects a systematic approach: find root cause → verify with isolated repro → fix all affected files globally → re-run full test suite → commit. Confidence: 0.9
- Prefers detailed commit messages with structured bullet points listing each fixed bug by invariant number and role name (e.g., "I9: 珪(Po)无结算产物"). Confidence: 0.85
- Delivers mega-tasks as single prompts bundling unrelated features (grimoire + mobile + post-game analysis) with explicit "全面自主交付" (fully autonomous delivery) mandate — expects the agent to self-heal all errors without ever pausing for human intervention. Confidence: 0.9
- Prefers version-milestone commits: each version tag (e.g., W8.19.2) bundles logically related changes into one commit, with one git push + one tag. Multi-feature deliverables are acceptable in a single commit when they share a version number. Confidence: 0.85
- UI must meet WCAG AA contrast standards for text readability — expects contrast checks as part of visual audit. Confidence: 0.9
- Expects an 8px spacing system (multiples of 8 for padding/margins/gaps) across all UI components. Confidence: 0.85
- Visual audits must cover multiple responsive viewports: desktop (1440x900), tablet (834x1194 portrait), and mobile (390x844). Confidence: 0.9
- Prefers `backdrop-blur` (backdrop-blur-md to backdrop-blur-xl) as a standard styling pattern for modal overlays, cards, and HUD elements over solid backgrounds. Confidence: 0.85
- Prefers pill-shaped backgrounds (rounded-md/rounded-lg with background color) for inline labels and action tags rather than plain colored text. Confidence: 0.85
- When expanding undo/redo state coverage, expects accompanying unit tests that verify serialization round-trip integrity (e.g., Set↔Array for non-JSON-serializable types). Confidence: 0.85
- UI layout rule: avoids absolute/fixed positioning for interactive toolbars and navigation — prefers inline document-flow layout (flex row/col, no overlay) to prevent z-index conflicts and element overlap. Confidence: 0.9
- Undo/redo must be atomic-action-level (原子动作级), not coarse stage-level. Each individual mutation (add reminder token, toggle death, confirm single night action, vote change) should be a separate undoable step. Confidence: 0.9
- Expects every state mutation point (including toggleStatus, reminder token add/remove, vote changes) to call saveHistory() for complete undo chains — any mutation that modifies game state without a snapshot is a bug. Confidence: 0.9
- When modifying a feature, expects corresponding unit tests to be added or updated in the same commit (not deferred). Build verification (`npm run build`) must also pass before commit. Confidence: 0.9
- Expects the agent to intelligently triage E2E failures: distinguish real code bugs (block deployment) from test-script issues like selector mismatches or phase-timing problems (acceptable to push with a note). Does not want every test failure treated as a blocking code defect. Confidence: 0.85
- Prefers creating reusable automation scripts (Node.js `scripts/` directory) for data calibration/migration tasks — comparison scripts, patch scripts, report generators — rather than doing manual field-by-field comparison or ad-hoc edits. Expects these scripts to be committed alongside the data changes. Confidence: 0.85
- Wants formal deliverable checklists/reports (e.g., 《校准完成清单》《全量角色语义校准总览》) output after each batch or milestone completion, summarizing what was changed, how many items were patched, and what gaps remain. Confidence: 0.85
- For large-scale analysis tasks, expects the agent to write comprehensive scan/classification scripts (e.g., semantic feature matrices, automated gap analysis) rather than manual review — the methodology itself should be programmatic and exhaustive. Confidence: 0.85
- Test suites organized by semantic/mechanic category (e.g., `01_illusions`, `02_candidates_and_bluffs`, `03_daytime_actions`, `04_swaps_and_mutations`, `05_multiphase_and_delays`, `06_overrides_and_endgame`) with zero-padded numeric prefixes for ordering. Files placed in `test_automation/semantic_deep_dive/`. Confidence: 0.85
- Expects reusable test helper factories (e.g., `makeSeat()`, `makeContext()`) defined at the top of each test file to construct minimal mock game objects, rather than inline object literals or external fixture files. Confidence: 0.8
- When implementing UI themes or visual redesigns, provides reference URLs (e.g., official game sites) and expects the agent to fetch and study the reference site's CSS/design tokens before writing any code — not to guess or invent a design language. Confidence: 0.9
- Will completely reject and redo visual implementations that don't match their quality bar, iterating through multiple full rewrites (e.g., 3+ rounds of theme redesign in one session). Expects the agent to accept rejection gracefully and pivot quickly without defending prior work. Confidence: 0.9
- Uses "commandcode --yolo" as a trigger to resume autonomous execution after the agent stalls or stops making tool calls. Confidence: 0.85
- Prefers CSS-scope-based theming (Tailwind v4 `@custom-variant`, `.theme-modern`/`.theme-classic` on `<html>`) over JavaScript-driven class toggling for theme systems. Expects theme variants to work reliably in production builds. Confidence: 0.85
- Extremely particular about text contrast on light backgrounds — considers any white/light text on a light background a "severe bug" (严重缺陷) requiring immediate emergency fix, not a cosmetic issue. Confidence: 0.95
- Prefers "base class + override" pattern for dual-theme styling: use the desired value as the default Tailwind class (e.g., `text-slate-900`), then override only in the alternate theme with scope selectors — rather than using `theme-modern:` variants for the default theme. Confidence: 0.85
- Official wiki examples are the authoritative test oracle: for each role, the user wants the agent to (1) understand ability from official wiki description, (2) use official examples to verify/correct code, (3) run real UI tests confirming every example triggers correctly. Tests must faithfully reproduce each official scenario, not just arbitrary edge cases. Confidence: 0.95
- Prefers per-script comprehensive testing: when focusing on a script (e.g., 罂粟花开), test ALL roles in that script exhaustively — every official example for every role — rather than sampling a few roles. Expects a coverage gap analysis (official examples vs. existing tests) before writing new tests. Confidence: 0.9
- Demands "双测试" (dual-test) coverage for every role: both vitest code-logic tests AND UI interaction tests must pass. "强制校验代码测试和UI交互测试，所有角色都必须双测试通过" — no role is considered covered unless both test types exist and pass. Confidence: 0.95
- "先不要上传" (don't push yet) is an explicit hold — do not git push to remote until the user explicitly approves. Local commits are fine, but remote sync requires user confirmation. Confidence: 0.9
- Prefers fixing code issues discovered during testing as part of the testing process itself (inline fix → re-run), not deferring fixes to a separate phase. The testing and fixing are one continuous loop. Confidence: 0.9
- For large-scale multi-role testing (e.g., 24 roles), prefers incremental one-at-a-time execution with human approval gates when exploring or stabilizing: test one role → report results → wait for user confirmation → proceed to next. However, once the process is proven stable ("鉴于测试已经稳定"), switches to batch mode — "一次性完成所有角色的测试" — demanding all roles pass dual-test coverage in one go. Confidence: 0.9
- When starting a large testing campaign, expects the agent to first produce a structured plan (coverage gap analysis + ordered checklist) before executing any tests. The plan should be presented for review before work begins. Confidence: 0.9
- Expects post-execution accountability: when asked about verification coverage, the agent must give a transparent, honest breakdown of what was actually verified vs. what remains untested — no inflating coverage claims. Wants tables showing ✅ covered and ❌ gaps with specific details. Confidence: 0.9
- When user says "再次检测确认" (re-verify) after a plan-completion claim, expects a read-only audit subagent to systematically re-check every plan item against actual code/UI files — not a self-affirming recap from the executor. The deliverable should be a 5-dimension (A/B/C/D/E) completion-rate matrix with status icons (✅/⚠️/❌/N/A), explicit in-scope vs out-of-scope partitioning, and concrete file:line evidence for each item. Confidence: 0.9
- Demands uniform modal behavior across ALL game roles — every role must have both a confirmation modal and a result modal during night actions. Inconsistent behavior (some roles with modals, some without) is treated as a design defect, not a feature. Confidence: 0.95
- Prefers full-screen overlay action pages during night phases that completely block background game state while keeping the top navbar accessible. Each role's action page should be a self-contained, isolated environment showing only that role's interactions and information. Confidence: 0.9
- For roles requiring target selection, expects the selection UI to be integrated directly into the content/result modal rather than being a separate interaction step. Confidence: 0.9
- When asking about a specific feature's behavior (e.g., "does poison take effect in real-time?"), expects the agent to proactively analyze ALL related operations (death, resurrection, drunk, etc.) exhaustively — not just the single case mentioned. Confidence: 0.9
- Provides detailed browser console logs when reporting UI bugs, expecting the agent to trace the full execution path from the logs rather than asking for reproduction steps. Confidence: 0.85
- Prefers incremental task scope expansion: starts with a specific bug report, then once the pattern is understood, expands to a comprehensive architectural requirement covering all similar cases. Expects the agent to anticipate this expansion. Confidence: 0.85
- UX navigation principle: the app should always open to a landing/selection page (e.g., script selection), never auto-navigate to a specific game state on load. Any session restoration must be presented as an explicit user choice via a styled prompt card with "继续" and "忽略" options. Confidence: 0.9
- When a search/grep fails to find the target pattern, the agent must adapt its strategy after 2-3 attempts (try different keywords, search in different directories, read the file directly) — never repeat the same search 10+ times. Getting stuck in a repetitive loop is a critical workflow failure. Confidence: 0.95
- For probabilistic game mechanics (e.g., 50% Recluse-as-evil in Chef/Fortune Teller, random bluff selection), prefers explicit `storytellerInput` injection hooks (e.g., `forceChefRecluseEvil: true | false`, `forceChefSpyGood: true | false`) inside the ability resolver rather than allowing `Math.random()` to make tests flaky. Pattern: helper functions like `resolveRecluseForChef(seatId, meta, ctx)` should read `(ctx as any)?.storytellerInput?.forceXxxEvil` first, fall back to `Math.random() < 0.5`, and persist the result in `meta` for stable retries. Tests then pass the `storytellerInput` field in the ctx object to deterministically control the random branch. Confidence: 0.9
- After a self-audit reveals "PARTIAL" or "NOT DONE" items, expects the user prompt "继续完成未完成的项目" to trigger systematic completion in priority order (core mechanics first, then UI, then tests) — not to stop at the audit report. Deferred items listed in CHANGELOG's "范围外" section are NOT a license to permanently skip them; they remain implicit work that the user may request completion of later. Confidence: 0.85

