# 血染钟楼系统迁移策略

## 当前状态分析

### ✅ 良好状态
- 所有测试通过 (17/17)
- src/ 目录无 TypeScript 类型错误
- 无循环依赖
- 项目运行稳定

### 🔄 重复架构现状

#### 1. 夜晚引擎
| 系统 | 状态 | 引用数 | 说明 |
|------|------|--------|------|
| `nightLogic.ts` | 生产使用 | 53处 | 当前主要系统 |
| `nightEngineFacade.ts` | 实验阶段 | 1处 | 未来方向 |
| `useNightLogic.ts` | 生产使用 | - | 旧版 Hook |
| `useNightEngine.ts` | 实验阶段 | - | 新版 Hook |

#### 2. 事件总线
| 系统 | 状态 | 说明 |
|------|------|------|
| `gameEventBus.ts` | 生产使用 | 基础系统 |
| `unifiedEventBus.ts` | 增强包装 | 基于 gameEventBus 构建 |

## 迁移策略

### 原则
1. **稳定性优先**：不破坏现有功能
2. **渐进迁移**：分阶段逐步替换
3. **持续测试**：每个阶段都验证完整性
4. **回滚安全**：保留旧代码直到完全验证

---

## 阶段一：准备期 (当前)

### 目标
- 建立统一导出层
- 添加明确的代码注释
- 确保所有测试通过

### 完成状态
- ✅ 已添加实验性质注释
- ✅ 已验证项目当前状态良好

---

## 阶段二：事件总线统一 (可选)

### 目标
- 让 unifiedEventBus 成为主要导出
- 保持向后兼容

### 实施步骤
1. 在 `src/utils/index.ts` 中统一导出
2. 让 `unifiedEventBus` 完全兼容 `gameEventBus` 接口
3. 逐步更新导入语句

---

## 阶段三：夜晚引擎增强

### 目标
- 扩展 `nightEngineFacade.ts` 功能
- 创建适配器层
- 保持接口兼容

### 实施步骤
1. 分析 `useNightLogic` 的完整接口
2. 创建 `NightLogicAdapter` 适配层
3. 实现缺失的功能
4. 逐个组件测试

---

## 阶段四：渐进式迁移

### 目标
- 逐个模块迁移到新系统
- 每个迁移都验证

### 优先级
1. 低风险模块优先
2. 单一职责模块优先
3. 有完整测试覆盖的模块优先

---

## 阶段五：清理 (最终)

### 目标
- 移除旧代码
- 更新文档

### 前提条件
- 100% 测试覆盖
- 完整的回归测试通过
- 至少一个完整游戏周期验证

---

## 风险评估

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 功能回归 | 高 | 中 | 完整测试覆盖 |
| 类型错误 | 中 | 低 | TypeScript 严格检查 |
| 迁移阻塞 | 中 | 低 | 保留旧代码可回滚 |

---

## 建议

### 当前方案 (推荐)
**保持现状**，原因：
1. 当前系统稳定运行
2. 新系统尚未完成
3. 迁移成本高，收益不明确

### 中期方案
1. 在新功能中优先使用新系统
2. 逐步增强新系统功能
3. 等待新系统成熟后再全面迁移

### 长期方案
1. 制定详细的迁移计划
2. 分阶段实施
3. 每个阶段都有完整测试
