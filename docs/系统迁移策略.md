# 血染钟楼系统迁移策略

## 当前状态分析（2026-06-20 更新）

### ✅ 良好状态
- 所有角色能力已迁移至新引擎并精修完成 (206个能力文件)
- 事件总线已统一 (unifiedEventBus)
- 夜晚顺序管理已统一 (unifiedNightOrder)
- 新引擎已在生产环境中使用
- TypeScript 类型检查通过 (0 errors)
- `npm run build` 构建成功
- 仿真测试 17/17 通过
- 单元测试 38/38 通过
- Playwright 配置优化：workers=1, headless=true
- E2E 测试：5 PASS, 7 SKIP（待重写）
- CodeGraph 集成：5,520 节点知识图谱

### ✅ 已完成迁移

| 模块 | 状态 | 说明 |
|------|------|------|
| **角色能力迁移** | ✅ 100% | 206个能力已注册并精修，覆盖所有官方+实验+自定义角色 |
| **新引擎上线** | ✅ 已完成 | `useNightEngine` 已在 `useGameController` 中使用 |
| **事件总线统一** | ✅ 已完成 | `unifiedEventBus` 已成为唯一的事件系统 |
| **夜晚顺序管理统一** | ✅ 已完成 | `unifiedNightOrder` 已成为统一的夜晚顺序管理器 |
| **骨架文件精修** | ✅ 已完成 | 134个骨架文件全部展开为完整 pipeline |
| **缺失能力补全** | ✅ 已完成 | 新增10个缺失能力文件 |
| **Bug 修复** | ✅ 已完成 | 黄昏→夜晚过渡修复、statusEffects空值修复等 |

### ⏳ 局部未完成

| 模块 | 状态 | 说明 |
|------|------|------|
| **队列生成逻辑** | ⏳ 待统一 | `wakeQueue` 深度集成，需架构级重构 |
| **旧引擎文件清理** | ✅ 已确认 | `nightLogic.ts` 保留备份（1个架构测试引用），`useNightLogic.ts` 已删除 |

### ✅ 2026-06-21 新增完成项

| 模块 | 说明 |
|------|------|
| **Jinx 相克规则系统** | jinxUtils `require()`→ES import，JinxManager 扩展10+规则绑定，21个专项测试 |
| **antagonism.ts TODO** | 利维坦第5天限制已实现（DAY_CONSTRAINED_CREATION + currentDay参数） |
| **NightOrderPreviewModal 警告** | props 解构修复，useEffect 依赖完整，构建零警告 |
| **测试覆盖提升** | +21 个 Jinx 专项测试，总测试数 59/61 |
| **旧 engine 文件** | useNightLogic.ts 已删除，nightLogic.ts 保留（1个架构测试依赖） |

### 📜 原始架构（已迁移）

#### 1. 夜晚引擎 — **已完成迁移**
| 系统 | 迁移前状态 | 当前状态 |
|------|-----------|----------|
| `nightEngineFacade.ts` | 实验阶段 | ✅ **生产使用** |
| `useNightEngine.ts` | 实验阶段 | ✅ **生产使用** |
| `nightLogic.ts` | 生产使用 (53处引用) | ⏸️ 保留备份 (已注释) |
| `useNightLogic.ts` | 生产使用 | ⏸️ 保留备份 (无外部导入) |

#### 2. 事件总线 — **已统一**
| 系统 | 迁移前状态 | 当前状态 |
|------|-----------|----------|
| `unifiedEventBus.ts` | 增强包装 | ✅ **已统一为唯一系统** |
| `gameEventBus.ts` | 基础系统 | ✅ **已作为类型别名** |

#### 3. 夜晚顺序管理 — **已统一**
| 系统 | 迁移前状态 | 当前状态 |
|------|-----------|----------|
| `unifiedNightOrder.ts` | 统一管理器 | ✅ **已统一为唯一管理器** |
| `nightOrderParser.ts` | 解析器 | ✅ 已整合 |
| `nightOrderOverrides.ts` | 覆盖配置 | ✅ 已整合 |

## 迁移策略（历史存档）

以下为原始迁移计划，**已基本全部完成**。

### 原则
1. **稳定性优先**：不破坏现有功能
2. **渐进迁移**：分阶段逐步替换
3. **持续测试**：每个阶段都验证完整性
4. **回滚安全**：保留旧代码直到完全验证

---

## 阶段一：准备期 ✅ **已完成**
- 建立统一导出层
- 添加明确的代码注释
- 确保所有测试通过

---

## 阶段二：事件总线统一 ✅ **已完成**
- unifiedEventBus 已成为主要导出
- 保持向后兼容

---

## 阶段三：夜晚引擎增强 ✅ **已完成**
- nightEngineFacade 功能已扩展
- 新引擎已完全接管生产环境

---

## 阶段四：渐进式迁移 ✅ **已完成**
- 逐个模块迁移到新系统
- 所有角色能力已迁移

---

## 阶段五：清理 ⏳ **部分完成**
- 旧代码保留为备份（未删除）
- 文档已更新

### 前提条件
- 角色能力迁移完成 ✅
- 事件总线统一完成 ✅
- 新引擎上线完成 ✅

### 剩余工作
- 清理旧队列生成逻辑 (`nightQueueGenerator` → `dynamicQueueGenerator`)
- 确认旧引擎文件 (`nightLogic.ts`, `useNightLogic.ts`) 是否可以完全移除

---

## 风险评估

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 功能回归 | 高 | 低 | 完整测试覆盖 |
| 类型错误 | 中 | 低 | TypeScript 严格检查 |
| 迁移阻塞 | 中 | 低 | 保留旧代码可回滚 |

---

## 建议

### 当前方案
**保持现状**，原因：
1. 当前系统稳定运行
2. 角色迁移已全部完成
3. 仅剩队列生成逻辑和旧文件清理两项待办

### 短期建议
1. 统一队列生成逻辑：将 `nightQueueGenerator` 剩余引用迁移至 `dynamicQueueGenerator`
2. 确认无障碍后移除旧引擎备份文件
3. 修复 `antagonism.ts` 中的残留 TODO
