# JSON文件夹保护准则

## 核心原则
此文件夹 (`/Users/jiajunchen/Documents/血染钟楼/GitHub/DZL/json`) 包含血染钟楼游戏的所有角色数据、剧本信息和官方规则文档。这些文件已经经过规范化处理，格式统一且完整。

## 修改禁令
**未经明确授权，禁止修改此文件夹内的任何文件。**

具体包括：
- `json/full/` 下的所有角色JSON文件
- `json/` 下的任何JSON、TXT、MD文件
- 任何现有的数据结构

## 例外情况
只有在以下情况下可以修改：
1. 用户 **明确指示** 需要更新某个特定角色或文件
2. 用户 **明确授权** 进行批量格式转换
3. 修复由外部工具引入的 **语法错误**（如引号不匹配、括号缺失）
4. 用户 **明确要求** 添加新角色或剧本

## 违反后果
未经授权修改此文件夹将导致：
- 破坏游戏数据的完整性
- 可能引起类型检查失败
- 破坏已经验证的测试用例
- 需要耗费大量token进行重新验证

## 行动指南
作为AI助手，当你收到涉及此文件夹的任务时：
1. **首先检查此文件** - 确认是否有修改授权
2. **向用户确认** - 如果没有明确指令，询问是否真的需要修改
3. **仅执行明确授权的修改** - 不做超出授权范围的更改
4. **修改后必须验证** - 运行 `npm run type` 和 `npm run test` 确保无破坏

## 历史记录
- 2026-03-30：完成对所有实验性角色JSON文件的规范化转换，格式统一为小恶魔示例结构

---
**此规则永久生效，作为游戏开发的基石准则。**