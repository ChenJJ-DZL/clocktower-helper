import { describe, expect, test } from "vitest";

describe("男爵 (Baron)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("设置阶段+2外来者", () => {
    expect(true).toBe(true);
  });
  test("替换镇民位", () => {
    expect(true).toBe(true);
  });
});
