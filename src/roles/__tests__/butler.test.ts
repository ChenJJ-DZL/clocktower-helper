import { describe, expect, test } from "vitest";

describe("管家 (Butler)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("每夜选主人-只有主人投票时才能投票", () => {
    expect(true).toBe(true);
  });
  test("首夜开始行动", () => {
    expect(true).toBe(true);
  });
});
