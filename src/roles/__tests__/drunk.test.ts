import { describe, expect, test } from "vitest";

describe("酒鬼 (Drunk)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("以为自己是镇民-实际不是", () => {
    expect(true).toBe(true);
  });
  test("能力不生效-信息可能错误", () => {
    expect(true).toBe(true);
  });
});
