import { describe, expect, test } from "vitest";

describe("小恶魔 (Imp)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("每夜选人杀害", () => {
    expect(true).toBe(true);
  });
  test("自杀可将恶魔传位给爪牙", () => {
    expect(true).toBe(true);
  });
  test("首夜不杀人(知爪牙)", () => {
    expect(true).toBe(true);
  });
});
