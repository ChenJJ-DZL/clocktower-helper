import { describe, expect, test } from "vitest";

describe("镇长 (Mayor)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("3人存活无处决-好人胜", () => {
    expect(true).toBe(true);
  });
  test("夜死可能被替死", () => {
    expect(true).toBe(true);
  });
  test("醉酒中毒时能力失效", () => {
    expect(true).toBe(true);
  });
});
