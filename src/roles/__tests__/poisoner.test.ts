import { describe, expect, test } from "vitest";

describe("投毒者 (Poisoner)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("每夜选一人-当晚+次日中毒", () => {
    expect(true).toBe(true);
  });
  test("中毒者得错误信息", () => {
    expect(true).toBe(true);
  });
});
