import { describe, expect, test } from "vitest";

describe("陌客 (Recluse)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("可能被视为邪恶/爪牙/恶魔", () => {
    expect(true).toBe(true);
  });
  test("死亡后仍生效", () => {
    expect(true).toBe(true);
  });
});
