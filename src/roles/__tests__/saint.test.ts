import { describe, expect, test } from "vitest";

describe("圣徒 (Saint)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("死于处决-阵营落败", () => {
    expect(true).toBe(true);
  });
  test("夜死不触发", () => {
    expect(true).toBe(true);
  });
});
