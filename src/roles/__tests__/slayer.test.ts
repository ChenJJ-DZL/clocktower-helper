import { describe, expect, test } from "vitest";

describe("猎手 (Slayer)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("白天公开选择-恶魔则死", () => {
    expect(true).toBe(true);
  });
  test("非恶魔无事发生", () => {
    expect(true).toBe(true);
  });
  test("每局一次", () => {
    expect(true).toBe(true);
  });
});
