import { describe, expect, test } from "vitest";

describe("贞洁者 (Virgin)", () => {
  test("Wiki-JSON", () => {
    expect(1).toBe(1);
  });
  test("首次被镇民提名-提名者处决", () => {
    expect(true).toBe(true);
  });
  test("非镇民提名不触发", () => {
    expect(true).toBe(true);
  });
  test("首次提名后能力消失", () => {
    expect(true).toBe(true);
  });
});
