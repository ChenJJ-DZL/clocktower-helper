import { RoleDefinition } from "../../types/roleDefinition";

/**
 * 哥布林
 * 如果被提名后公开自称哥布林且被处决，则邪恶获胜。
 * 仅作为占位，完整结算交给说书人流程。
 */
export const goblin: RoleDefinition = {
  id: "goblin",
  name: "哥布林",
  type: "minion",
};


