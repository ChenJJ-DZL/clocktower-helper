import type{ MiddlewareContext } from"../../utils/middlewarePipeline";import{ AbilityTriggerTiming,createRoleAbility}from"../core/roleAbility.types";
const pc=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{return ctx};
const calc=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const deads=ctx.snapshot.seats.filter((s:any)=>s.isDead&&s.role);const t=ctx.storytellerInput?.targetId??(deads.length>0?deads[Math.floor(Math.random()*deads.length)].id:null);return{...ctx,meta:{...ctx.meta,abilityResult:{targetId:t,ferried:t!==null}}}};
const su=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const r=ctx.meta.abilityResult as any;if(!r?.ferried)return ctx;return{...ctx,snapshot:{...ctx.snapshot,_abilityResults:{...((ctx.snapshot as any)._abilityResults??{}),ferryman:r}},meta:{...ctx.meta,ferrymanResult:r}};};
const pp=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const r=ctx.meta.abilityResult as any;console.log(r?.ferried?`[Ferryman] 摆渡${r.targetId+1}号`:"[Ferryman] 无目标");return ctx;};
export const ferrymanAbility=createRoleAbility({roleId:"ferryman",abilityId:"ferryman_cross",abilityName:"灵魂摆渡",triggerTiming:[AbilityTriggerTiming.EVERY_NIGHT],wakePriority:90,firstNightOnly:false,wakePromptId:"role.ferryman.wake",targetConfig:{min:0,max:1,allowSelf:false,allowDead:true},preCheck:[pc],calculate:[calc],stateUpdate:[su],postProcess:[pp]});