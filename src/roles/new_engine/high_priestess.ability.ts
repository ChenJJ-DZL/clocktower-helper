import type{ MiddlewareContext } from"../../utils/middlewarePipeline";import{ AbilityTriggerTiming,createRoleAbility}from"../core/roleAbility.types";
const pc=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const s=ctx.snapshot.seats.find((s:any)=>s.id===ctx.actionNode.seatId);if(!s?.isAlive)return{...ctx,aborted:true,abortReason:"已死亡"};return ctx};
const calc=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const candidates=ctx.snapshot.seats.filter((s:any)=>s.id!==ctx.actionNode.seatId);const t=ctx.storytellerInput?.targetId??candidates[Math.floor(Math.random()*candidates.length)]?.id;return{...ctx,meta:{...ctx.meta,abilityResult:{targetId:t}}}};
const su=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{return{...ctx,meta:{...ctx.meta,highPriestessResult:ctx.meta.abilityResult}};};
const pp=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const r=ctx.meta.abilityResult as any;console.log(r?.targetId?`[HighPriestess] 指引${r.targetId+1}号`:"[HighPriestess] 无指引");return{...ctx,meta:{...ctx.meta,abilityLog:r?.targetId?`指引${r.targetId+1}号`:"无指引"}};};
export const high_priestessAbility=createRoleAbility({roleId:"high_priestess",abilityId:"high_priestess_guide",abilityName:"高女祭司",triggerTiming:[AbilityTriggerTiming.EVERY_NIGHT],wakePriority:30,firstNightOnly:false,wakePromptId:"role.high_priestess.wake",targetConfig:{min:0,max:0,allowSelf:false,allowDead:true},preCheck:[pc],calculate:[calc],stateUpdate:[su],postProcess:[pp]});