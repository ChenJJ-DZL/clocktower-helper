import type{ MiddlewareContext } from"../../utils/middlewarePipeline";import{ AbilityTriggerTiming,createRoleAbility}from"../core/roleAbility.types";
const pc=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{return ctx};
const calc=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const demon=ctx.snapshot.seats.find((s:any)=>!s.isDead&&s.role?.type==="demon");return{...ctx,meta:{...ctx.meta,abilityResult:{marionetteActive:true,demonSeatId:demon?.id??null,thinksTheyAreGood:true}}}};
const su=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{const r=ctx.meta.abilityResult as any;return{...ctx,snapshot:{...ctx.snapshot,marionetteActive:true,marionetteMaster:r?.demonSeatId},meta:{...ctx.meta,marionetteResult:r}};};
const pp=async(ctx:MiddlewareContext):Promise<MiddlewareContext>=>{console.log("[Marionette] 提线木偶激活");return ctx;};
export const marionetteAbility=createRoleAbility({roleId:"marionette",abilityId:"marionette_passive",abilityName:"提线木偶",triggerTiming:[AbilityTriggerTiming.PASSIVE],wakePriority:0,firstNightOnly:false,wakePromptId:"",targetConfig:{min:0,max:0,allowSelf:false,allowDead:false},preCheck:[pc],calculate:[calc],stateUpdate:[su],postProcess:[pp]});