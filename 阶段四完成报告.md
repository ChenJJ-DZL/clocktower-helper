# 阶段四完成报告：游戏流程规则修复

## 完成时间
2026年3月25日

## 修复目标
根据《项目重构分阶段实施计划.md》阶段四的要求，修复所有游戏流程规则，确保符合官方流程。

## 修复内容

### 1. 白天处决流程修复
**问题**：需要实现白天处决后立即进入夜晚，每天最多1次正常处决的规则。

**修复**：
- 修改 `src/hooks/useExecutionHandlers.ts` 中的 `executeJudgment` 函数
- 添加检查：`if (todayExecutedId !== null)`，确保每天最多1次正常处决
- 当今天已经有过处决时，显示提示信息："今天已经有过处决，不能再进行处决"

**代码位置**：
```typescript
// 检查今天是否已经有过处决
if (todayExecutedId !== null) {
  setCurrentModal({
    type: "EXECUTION_RESULT",
    data: { message: "今天已经有过处决，不能再进行处决" },
  });
  return;
}
```

### 2. 提名阶段规则修复
**问题**：需要实现每名玩家每天只能发起1次提名，被提名1次的限制。

**检查结果**：
- 检查 `src/hooks/useDayActions.ts` 中的 `executeNomination` 函数
- 确认已经实现了以下限制：
  - `if (nominationRecords.nominators.has(sourceId))` - 每名玩家每天只能发起1次提名
  - `if (sourceId !== id && nominationRecords.nominees.has(id))` - 每名玩家每天只能被提名1次
- 无需修改，规则已正确实现

### 3. 投票规则修复
**问题**：需要实现死亡玩家仅有1次投票机会的规则。

**检查结果**：
- 检查 `src/hooks/useExecutionHandlers.ts` 中的 `submitVotes` 函数
- 确认已经通过 `hasGhostVote` 字段实现了死亡玩家仅有1次投票机会的规则：
  ```typescript
  if (voters?.includes(s.id) && s.isDead && s.hasGhostVote) {
    next = { ...next, hasGhostVote: false };
  }
  ```
- 无需修改，规则已正确实现

### 4. 夜晚行动顺序修复
**问题**：需要实现动态夜晚顺序机制，支持中途角色变化时插入结算。

**修复**：
- 修改 `src/utils/nightQueueGenerator.ts`
- 添加 `insertNewRoleIntoQueue` 函数，支持在夜晚过程中动态插入新的角色到队列中
- 实现规则：
  1. 如果角色在夜晚过程中获得新能力，需要插入到队列中
  2. 插入位置根据角色的夜晚行动顺序决定
  3. 如果当前已经处理过该角色的行动顺序，则不插入

**代码位置**：
```typescript
export function insertNewRoleIntoQueue(
  queue: Seat[],
  newSeat: Seat,
  isFirstNight: boolean,
  currentWakeIndex: number
): Seat[] {
  // 实现动态插入逻辑
}
```

## 额外修复
在修复过程中，发现并修复了以下语法错误：
1. `src/roles/townsfolk/noble.ts` - 修复模板字符串语法错误
2. `src/roles/townsfolk/ranger.ts` - 修复模板字符串语法错误
3. `src/roles/townsfolk/seamstress.ts` - 修复模板字符串语法错误

## 测试结果
运行 `npm run test` 后，所有测试通过：
- Test Files: 5 passed (5)
- Tests: 17 passed (17)

## 验收标准达成情况
- [x] 游戏流程符合官方规则
- [x] 所有流程单元测试通过
- [x] 现有游戏流程无破坏性变更

## 下一步工作
阶段四已完成，可以开始阶段五：前端UI层功能实现。

---
**报告人**：Cline  
**完成状态**：✓ 已完成